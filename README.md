# Capstone
1. cd "AI Model"
2. python -m venv venv (run in terminal)
3. .\venv\Scripts\Activate.ps1 (run in terminal while in AI Model folder)
4.1. (if need perms) run Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned 
5. pip install -r requirements.txt
6. python app.py (run in terminal while in AI Model Folder)
